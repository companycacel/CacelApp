<Window x:Class="CacelApp.Shared.Controls.PdfViewer.PdfViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        Style="{StaticResource MaterialDesignWindow}"  
        Focusable="True"
        KeyDown="PdfViewerWindow_KeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Content Area -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="{DynamicResource MaterialDesignDividerBrush}"
                BorderThickness="1,0,1,0">

            <Grid>
                <!-- WebView2 para mostrar PDF -->
                <wv2:WebView2 x:Name="PdfWebView"
                             Visibility="{Binding IsDocumentLoaded, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Loading Indicator -->
                <StackPanel VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                Value="0"
                                IsIndeterminate="True"
                                Width="60" Height="60"/>
                    <TextBlock Text="Cargando PDF..." 
                              Margin="0,20,0,0"
                              FontSize="14"
                              HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Error Message -->
                <StackPanel VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <materialDesign:PackIcon Kind="AlertCircle" 
                                           Width="64" Height="64"
                                           Foreground="{DynamicResource ValidationErrorBrush}"/>
                    <TextBlock Text="Error al cargar el PDF" 
                              Margin="0,20,0,0"
                              FontSize="16"
                              FontWeight="SemiBold"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ErrorMessage}" 
                              Margin="0,10,0,0"
                              FontSize="12"
                              TextWrapping="Wrap"
                              MaxWidth="450"
                              TextAlignment="Center"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</Window>
